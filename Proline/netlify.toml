[build]
    command = "pnpm build"
    publish = "dist"

[build.environment]
    NODE_VERSION = "22"

[images]
    remote_images = [
        "https?://maps\\.googleapis\\.com/.*",
        "https://[a-z0-9.-]+(:[0-9]+)?(\\/[^?#]*)?([?][^#]*)?",
    ]
