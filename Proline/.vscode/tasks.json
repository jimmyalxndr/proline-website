{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pnpm dev",
            "type": "shell",
            "command": ". ~/.nvm/nvm.sh; nvm use; pnpm dev",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
